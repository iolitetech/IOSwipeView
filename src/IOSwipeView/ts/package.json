{
  "name": "ioswipeview-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript runtime engine for IOSwipeView Blazor component.",
  "scripts": {
    "build": "npx --yes tsc -p ./tsconfig.json",
    "watch": "npx --yes tsc -p ./tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
